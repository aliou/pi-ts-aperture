{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/ApertureConfig",
  "definitions": {
    "ApertureConfig": {
      "type": "object",
      "properties": {
        "$schema": {
          "type": "string"
        },
        "version": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0
            },
            {
              "type": "string",
              "pattern": "^\\d{1,15}(\\.\\d{1,15})?(\\.\\d{1,15})?$"
            }
          ],
          "description": "Config schema version, stamped by migrations. Current version: 0.11.4."
        },
        "baseUrl": {
          "type": "string",
          "description": "Aperture gateway base URL (e.g. `https://aperture.example.com`)."
        },
        "openaiRoute": {
          "$ref": "#/definitions/OpenAiRoute",
          "description": "Public route prefix for OpenAI-compatible models. `v1` is the standard Aperture route; use `root` for deployments exposing `/chat/completions` and `/responses` directly at the gateway origin."
        },
        "onboardingDone": {
          "type": "boolean",
          "description": "Whether onboarding has been completed."
        },
        "onboarding": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Whether the onboarding extension affordances are active."
            }
          },
          "additionalProperties": false
        },
        "proxy": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Reroute selected Pi providers through Aperture."
            },
            "upstreamProviders": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProxiedProviderConfig"
              }
            }
          },
          "additionalProperties": false
        },
        "dedicated": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Register a standalone `aperture` provider from gateway models."
            },
            "providers": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DedicatedProviderConfig"
              }
            }
          },
          "additionalProperties": false
        },
        "connectors": {
          "$ref": "#/definitions/ConnectorsConfig"
        }
      },
      "additionalProperties": false
    },
    "OpenAiRoute": {
      "type": "string",
      "enum": [
        "v1",
        "root"
      ],
      "description": "Public path variant used by OpenAI-compatible Aperture routes."
    },
    "ProxiedProviderConfig": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Aperture provider id (matches `/api/providers` response)."
        },
        "enabled": {
          "type": "boolean",
          "description": "Proxy this provider through Aperture (default true). Set false to keep per-provider settings without proxying the provider."
        },
        "shouldCheckGatewayModels": {
          "type": "boolean",
          "description": "Warn when configured local models are missing from the Aperture gateway."
        },
        "keepGatewayModelsOnly": {
          "type": "boolean",
          "description": "Only register models the gateway actually serves for this provider."
        },
        "api": {
          "$ref": "#/definitions/RoutableApi",
          "description": "Route this provider's models through a specific Pi API instead of the auto-detected one. Validated against the gateway compatibility map on every sync; falls back to auto with a warning when not served."
        }
      },
      "required": [
        "id"
      ],
      "additionalProperties": false
    },
    "RoutableApi": {
      "type": "string",
      "enum": [
        "openai-completions",
        "openai-responses",
        "anthropic-messages",
        "bedrock-converse-stream",
        "google-generative-ai",
        "google-vertex"
      ],
      "description": "Pi APIs derivable from a gateway provider's compatibility map. Extracted from pi-ai's `KnownApi`: the JSON schema needs a closed union (the open `Api` type would degrade to a plain string), and the remaining KnownApi members can never come from a compatibility map."
    },
    "DedicatedProviderConfig": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Aperture provider id."
        },
        "name": {
          "type": "string",
          "description": "Optional display name override."
        },
        "enabled": {
          "type": "boolean",
          "description": "Include this provider's models in the dedicated provider."
        },
        "api": {
          "$ref": "#/definitions/RoutableApi",
          "description": "Route this provider's models through a specific Pi API instead of the one auto-picked from the compatibility map. Validated on every catalog refresh; falls back to auto with a warning when not served. Part of the catalog key, so cached catalogs under a different api are never replayed."
        }
      },
      "required": [
        "id",
        "enabled"
      ],
      "additionalProperties": false
    },
    "ConnectorsConfig": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Master switch for the connectors feature. When `false`, the connectors extension registers nothing. Defaults to `false`."
        },
        "pinnedTools": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PinnedConnectorTool"
          },
          "description": "MCP tools to register as first-class Pi tools instead of via the discovery meta-tools. Matching is by `toolName`; stale entries are silently skipped on registration.\n\nEach pinned tool adds its full JSON Schema to the system prompt, so prefer pinning only the few tools you use every session."
        },
        "discoveryTools": {
          "type": "boolean",
          "description": "Register the connector discovery meta-tools (list / search / describe / call).\n\nWhen `false`, only pinned tools are registered as first-class Pi tools. Decorrelated from `enabled`, which still gates whether pinning runs at all. Defaults to `true`."
        }
      },
      "additionalProperties": false,
      "description": "Connector tools configuration.\n\n`enabled` gates the entire connectors feature: when `false`, no connector tools (pinned or discovery) are registered. It replaces the former `features.connectors` flag."
    },
    "PinnedConnectorTool": {
      "type": "object",
      "properties": {
        "connectorId": {
          "type": "string",
          "description": "Connector id exposing the tool (tool name prefix before the first `_`)."
        },
        "toolName": {
          "type": "string",
          "description": "MCP tool name (from Aperture `/v1/mcp` `tools/list`), matched verbatim."
        }
      },
      "required": [
        "connectorId",
        "toolName"
      ],
      "additionalProperties": false,
      "description": "A pinned connector tool entry.\n\n`toolName` is matched verbatim against the MCP tool name returned by Aperture's /v1/mcp endpoint. `connectorId` is the connector that exposes the tool (the tool name prefix before the first `_`). It is stored for traceability; matching is done by `toolName`.\n\nPinned tools are registered as first-class Pi tools instead of being reached through the connector proxy meta-tools. Entries whose `toolName` no longer exists on the gateway are silently skipped on registration, so stale entries are harmless.\n\nEach pinned tool adds its full schema to the system prompt, which raises context cost. Prefer pinning only the few tools you use every session."
    }
  }
}
